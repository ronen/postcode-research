# Development Conventions

## `_` Directories for Temporary and Disposable Files

Root-level directories whose names begin with an underscore are excluded from Git by the root `.gitignore`; temporary or disposable files should be placed in an appropriately named root-level directory rather than scattered through the repository.
